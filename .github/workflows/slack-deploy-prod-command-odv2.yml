name: Slack Deployment of PROD Command for ODv2

on:
  push:
    branches:
      - master

jobs:
  deploy-to-prod:
    uses: ./.github/workflows/deploy-to-production.yml
    with:
      prdFileName: "rc.tar.gz"
    secrets:
      HOST: ${{ secrets.HOST }}